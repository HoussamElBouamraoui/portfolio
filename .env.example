np# Frontend
# Optionnel en dev: si vous lancez `vercel dev` sur le port 3000, laissez vide et Vite proxy /api -> 3000
# Sinon, vous pouvez mettre l’URL complète de l’API (ex: https://votre-domaine.vercel.app)
VITE_API_BASE_URL=

# Backend (fonctions serverless /api)
# Clé API Resend (https://resend.com/)
RESEND_API_KEY=re_GayGWWyo_FZQo8WDoMZ4PHXptmmtieKHX
# Adresse d’expéditeur (doit être vérifiée chez Resend)
MAIL_FROM=onboarding@resend.dev
# Adresse de réception
MAIL_TO=houssamelbouamraoui@gmail.com

# CORS (optionnel): en prod, remplacez * par votre domaine exact
CORS_ORIGIN=*
